#!/usr/bin/perl
use lib '..';

#print "$_\n" for @INC;

#my $do = DigitalOcean->new;#->new(client_id=> $ENV{CLIENT_ID}, api_key => $ENV{API_KEY});
#print "$do\n";

#print droplets() . "\n";

#sub droplets {
#	return $do->droplets;
#}
